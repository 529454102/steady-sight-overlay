{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the settings and overlay windows.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
